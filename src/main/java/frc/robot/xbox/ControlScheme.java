package frc.robot.xbox;


/**
 * This class is used to store the control scheme of the robot, which will eventually be referenced further in {@link frc.robot.RobotContainer}.
 * Each VARIABLE_NAME relates to the command performed by
 * {@link ControlScheme.button}
 * */
public enum ControlScheme {
    /*  Example:
    VARIABLE_NAME(DRIVER.getLeftBumper())
    SHOOT(DRIVER.getA())

    public final Trigger button;

    ControlScheme(Trigger button) {
        this.button = button;
    }  */
}
